package _008_while_loop

object MainObject {
  def main(args: Array[String]): Unit = {
    var a = 10 // Initialization
    while (a <= 20) { // Condition
      println(a)
      a = a + 2 // Incrementation
    }
  }
}  